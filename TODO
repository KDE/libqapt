-Thread Backend interactions with the apt cache. While not too slow, it's slow enough to warrant the use of threading to prevent GUI blockage.
-Make the direct output of the dpkg process available, so that advanced package managers can have a console GUI for power users
-Add the ability to cancel package installation/removal/upgrades
-grep -iR "FIMXE" src/
-grep -iR "TODO" src/
;-)

